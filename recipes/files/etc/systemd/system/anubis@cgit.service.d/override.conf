[Service]
# Appended to the EnvironmentFile of the packaged anubis@.service unit. The
# leading "-" keeps the unit startable before the key has been generated.
# systemd reads this as root, so the file can stay mode 600.
EnvironmentFile=-/etc/anubis/cgit.secret.env
